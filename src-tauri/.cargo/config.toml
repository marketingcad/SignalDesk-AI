# Ensure MSVC linker is used instead of Git's link.exe on Windows
[target.x86_64-pc-windows-msvc]
linker = "C:\\Program Files (x86)\\Microsoft Visual Studio\\2022\\BuildTools\\VC\\Tools\\MSVC\\14.44.35207\\bin\\Hostx64\\x64\\link.exe"

# Use user-local target directory to avoid path/permission issues
[build]
target-dir = "target"
